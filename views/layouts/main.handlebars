<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Paytone+One&display=swap" rel="stylesheet">
    <title>Burger Tracker</title>
    <style>
        body {
            background-color: rgb(253, 253, 253);
        }

        .jumbotron {
            background-color: rgba(53, 57, 255, 0.699);
        }

        .jumboSign {
            font-family: 'Paytone One', sans-serif;
        }

        .burgerTitles {
            margin-top: 2%;
            display: inline-block;
        }

        .availableBrgrs {
            padding-top: 0%;
        }

        .burgerBtn {
            display: inline-block;
        }

        .burgerOrder {
            margin-top: 2%;
        }

        .orderBrgrBtn {
            margin-top: 2%;
        }
    </style>
</head>
<body>

    <div class="container container-fluid mt-1">
        <div class="jumbotron text-center mt-1 text-white">
            <h1 class="display-4 jumboSign">Welcome to Virtual Burger</h1>
        </div>
        {{!-- three curly braces indicates directions to render the html --}}
        {{{body}}}
    </div>

    <div class="container">
        <div class="row text-center" id="question-box">
            {{!-- <div class="col-md-3"></div>  --}}
            
            {{!-- Burger order form --}}
            <div class="card-body form-group bg-secondary text-white burgerOrder">
                <form action="burger/create" method="POST">
                    <label for="newBurger">Request a new burger by placing your order below</label>
                    <input type="text" name="burger_name" class="form-control" value="" id="burger_request">
                    <button id="submit-button" type="submit" class="btn btn-danger btn-lg orderBrgrBtn">Place Order</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>

